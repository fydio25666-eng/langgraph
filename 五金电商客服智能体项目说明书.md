# 五金电商客服智能体项目说明书

文档版本：2026-09-22

## 1. 项目是什么

这是一个面向五金、家居和汽车底盘配件电商场景的智能客服项目。用户可以通过命令行、HTTP 接口或网页向系统提问，系统会根据本地售后手册和订单工具生成结构化客服回复。

项目解决的主要问题：

- 自动回答商品、材质、规格、安装、物流和售后问题。
- 把售前、售后、退款和物流问题分成不同处理流程。
- 回答尽量依据已确认资料，减少臆测和幻觉。
- 模型调用失败、参数错误或无法确认时，能够重试并转人工。
- 保存会话历史，支持连续对话。
- 提供接口鉴权、日志、健康检查、测试和自动化评测。

它适合用于五金电商客服原型、内部客服工具、商品知识库问答系统、售前咨询和售后分流，也适合作为 LangGraph 工作流、RAG 和结构化模型输出的示例项目。

当前项目中的订单和退款服务是演示适配器，不是真实的生产订单系统。接入生产环境时，需要替换对应服务实现和数据权限校验。

## 2. 项目从 Demo 到完整系统的演进

### 2.1 最初 Demo：验证最小闭环

最初的 Demo 把所有逻辑放在一个 Python 文件中，包含：

1. 定义客服回复格式。
2. 读取模型配置。
3. 接收用户问题。
4. 让模型识别售前、售后或其他意图。
5. 使用 LangGraph 根据意图分支。
6. 模型异常时返回人工兜底。

这个阶段的重点是验证“输入问题后能得到结构化结果”，不是追求功能完整。

### 2.2 为什么要拆分

随着订单、知识库、会话和 HTTP 功能增加，单文件会同时承担配置、数据模型、业务节点、流程编排、接口和持久化，修改一个功能容易影响其他功能。因此项目按职责拆分：

| 文件 | 主要职责 |
| --- | --- |
| demo.py | 保留最小单文件示例，便于理解和演示 |
| config.py | 读取环境变量，创建模型客户端 |
| schemas.py | 定义请求、回复、订单、退款和工作流状态 |
| nodes.py | 实现分类、检索、售前、售后和人工节点 |
| graph.py | 组装、路由并编译 LangGraph |
| checkpoint.py | 使用 SQLite 保存会话检查点 |
| app.py | 提供 FastAPI、鉴权、JSON、SSE 和日志 |
| main.py | 提供命令行调用入口 |
| rag.py | 加载手册、切分文档、生成向量和混合检索 |
| tests/ | API 测试和模型质量评测 |

核心关系是：app.py 或 main.py 调用 graph.py；graph.py 调用 nodes.py 和 checkpoint.py；nodes.py 使用 config.py 的模型、rag.py 的知识库以及订单和退款服务。

### 2.3 Demo 与完整入口的关系

demo.py 是教学和快速演示实现，展示单文件如何完成分类和图路由。

完整业务入口是：

- 命令行：main.py
- HTTP 服务：app.py
- 工作流：graph.py

新增生产能力时应修改对应模块，不要把所有逻辑继续堆回 demo.py。

## 3. 系统整体架构

系统分为六层：

1. 接入层：FastAPI、命令行和网页。
2. 鉴权层：校验 API Token。
3. 工作流层：LangGraph 负责节点和条件路由。
4. 能力层：模型、RAG、订单查询和退款工具。
5. 状态层：SQLite 保存会话检查点，Chroma 保存本地知识库。
6. 运维层：日志、健康检查、测试和 Docker 部署。

一次请求的主流程：

用户问题 -> POST /chat 或 main.py -> 参数和 Token 校验 -> graph.app.invoke/stream -> prepare_history -> retrieve -> classify -> 售前或售后节点 -> finalize_history -> JSON 或 SSE 返回。

分类失败会重试或转人工；模型回复失败只重试当前业务节点。

## 4. 一次请求的详细流程

### 4.1 请求进入 FastAPI

客户端调用 POST /chat 时：

1. FastAPI 校验 question、thread_id 和 stream。
2. question 会去除首尾空格，空问题返回 422。
3. question 最长 1000 个字符。
4. require_api_token 检查 Authorization: Bearer API_TOKEN。
5. stream 为 false 时返回单个 JSON。
6. stream 为 true 时返回 Server-Sent Events。

健康检查接口是 GET /health，成功返回 {"status": "ok"}。

### 4.2 prepare_history

这个节点清理当前问题，将用户消息追加到历史，只保留最近 10 条，并把 retry_count 和 retryable_error 重置。

### 4.3 retrieve

这个节点调用 rag.retrieve()，为后续模型提供相关手册片段和来源编号。检索异常不会直接让服务崩溃，而是返回安全提示，让模型避免凭空猜测。

### 4.4 classify

分类模型返回结构化 ServiceReply，并识别：

- presales：商品、价格、库存、材质、规格、配送等售前问题。
- after_sales：退款、退货、物流、签收和售后问题。
- other：无法归入前两类的问题。

模型格式错误、调用失败或 Pydantic 校验失败时，节点标记 retryable_error，交给图路由处理。

### 4.5 路由和重试

classify 失败最多重试 3 次，达到上限进入 human 节点。售前或售后节点失败时，只重试当前节点，不重新执行整个流程。成功、other 和 human 分支最后都进入 finalize_history。

### 4.6 售前节点

售前节点只允许依据知识库上下文回答商品、价格、库存和配送信息，并将检索来源写入 sources 字段，最多保留 5 个来源。

### 4.7 售后节点

售后节点处理退款、退货、物流、签收异常和少件破损。如果问题包含 ORD- 加数字的订单号，会先进行 Pydantic 校验，再调用订单查询工具。如果同时包含退款金额和退款意图，会构造退款请求并校验金额、订单和幂等键。模型不得承诺订单系统没有确认的退款结果或物流时效。

## 5. 配置和核心数据

### 5.1 环境变量

项目根目录使用 .env：

    BASE_URL=你的模型服务地址
    API_KEY=你的模型密钥
    MODEL=你的模型名称
    API_TOKEN=接口访问令牌
    SQLITE_CHECKPOINT_PATH=data/checkpoints.sqlite

前三项用于创建 ChatOpenAI。缺少任意一项时，模型变量为 None，业务节点会转人工。API_TOKEN 用于保护 /chat。SQLITE_CHECKPOINT_PATH 不配置时默认使用 data/checkpoints.sqlite。

### 5.2 ServiceReply

所有业务分支使用相同的回复结构：intent、sub_intent、reply、need_human、risk_level 和 sources。结构化输出让 API、路由和测试不依赖自由文本解析。

### 5.3 SupportState

工作流状态包括 question、result、messages、retry_count、retryable_error、knowledge_context 和 knowledge_sources。

## 6. 知识库和 RAG

### 6.1 手册内容

商品售后手册.txt 是主要事实来源，覆盖发货、快递、运费、发票、退换货、轻钢龙骨、手动工具、卫浴置物架、汽车减振顶胶以及知识库外问题的兜底规则。

### 6.2 建立向量库

首次检索时，rag.py 会检查手册，创建 .chroma 目录，按段落切分文本，再组合成约 800 字的文档块，使用本地 HashEmbedding 生成 256 维向量并写入 Chroma。集合为空或分块数量变化时自动 upsert。

### 6.3 混合检索

检索同时使用 Chroma 向量相似度和中文单字、二元词组关键词匹配。向量检索适合语义相近表达，关键词检索适合精确参数和专有名词。综合排序后返回 Top-K 文本和来源。

### 6.4 RAG 故障排查

先执行：

    python -c "from rag import retrieve; print(retrieve('现货商品什么时候发货？'))"

正常情况应看到手册内容和来源。如果返回知识库不可用，检查手册文件、.chroma 读写权限、collection 文档数量和 HashEmbedding 的 embed_documents/embed_query。Chroma 1.x 可能以 list[str] 调用 embed_query，不能把列表直接调用 lower()。

## 7. 订单、退款和人工边界

order_service.py 当前是演示订单服务，生产环境需要替换成带身份和订单归属校验的真实服务。

refund_service.py 展示金额、可退款余额、订单和幂等校验。真实支付环境还应增加审计记录、人工审批和权限控制。

以下情况进入人工：模型配置缺失、模型连续校验失败、知识库无法确认、订单或退款参数不安全、问题超出知识库范围。人工兜底的作用是在不能可靠回答时停止猜测。

## 8. 安装、运行和使用

### 8.1 安装

    python -m venv .venv
    .venv\Scripts\Activate.ps1
    pip install -r requirements.txt

### 8.2 命令行

    python main.py "现货商品什么时候发货？"

返回结构化 JSON，包含意图、回复、是否需要人工和风险等级。

### 8.3 HTTP 服务

    uvicorn app:app --host 127.0.0.1 --port 8000

调用时需要请求头 Authorization: Bearer 你的API_TOKEN，请求体例如：

    {"question":"现货商品什么时候发货？","stream":false}

stream 改为 true 时使用 SSE 流式返回。

### 8.4 Docker

    docker compose up --build

容器使用 Python 3.11 和非 root 用户；Chroma、SQLite 使用持久化卷；根文件系统只读；/health 用于健康检查。

## 9. 测试和质量评测

### 9.1 接口测试

    python -m unittest discover -s tests -p "test_*.py"

覆盖健康检查、售前、售后、空问题、退款金额边界和模型异常。

### 9.2 20 道题评测

    python tests/eval_model.py

每题使用独立 thread_id 调用真实 graph，得到 Agent 实际回答，再由配置的大模型评分，输出准确度、忠诚度/幻觉率、回答相关度、语气合规度和综合平均分。

如果大量题目显示知识库不可用，应先单独检查 rag.retrieve()，不要把检索故障直接当成模型能力问题。

## 10. 日志、监控和运行数据

app.py 使用 Python logging，同时输出到终端和项目根目录 app.log。全局 HTTP 中间件记录请求方法、路径、状态码和耗时，异常处理器记录校验错误和未处理异常。

healthcheck.py 请求 /health，Docker 根据检查结果判断服务是否正常。

以下属于本地运行数据，不应上传 GitHub：.chroma/、data/*.sqlite、app.log 和 .env。当前 .gitignore 已忽略这些内容；SQLite 可能包含用户问题和会话内容。

## 11. 常见问题

### 知识库暂时不可用

先直接调用 rag.retrieve()。如果异常，检查 Chroma、手册和 Embedding 查询参数，尤其是 list[str] 与单字符串的兼容。

### 模型配置不完整

检查 .env 中 BASE_URL、API_KEY 和 MODEL。缺少配置时转人工是预期的安全行为。

### 接口返回 401

检查 API_TOKEN 和 Authorization: Bearer 令牌是否一致。

### 接口返回 422

检查 question 是否为空、是否超过 1000 字，以及是否传入了额外字段。

### 模型连续校验失败

检查模型是否支持 JSON 模式和结构化输出，返回字段是否符合 ServiceReply 的枚举和长度限制。

### Git push 被拒绝

远程有本地没有的提交时执行：

    git fetch origin
    git rebase origin/master
    git push origin master

网络重置时检查 git remote -v 和 Test-NetConnection github.com -Port 443。不要使用 git push --force 覆盖远程 master。

## 12. 推荐的日常开发流程

1. git status 查看分支和工作区。
2. git fetch origin 获取远程变化。
3. git rebase origin/master 保持历史清晰。
4. 修改一个明确功能。
5. 先做局部检查，再运行对应测试。
6. 修改 RAG 时直接检查 retrieve()。
7. 修改图时检查 retry、human 和 finalize_history 出口。
8. 确认 .env、.chroma、SQLite 和日志没有被跟踪。
9. git add、git commit。
10. git push origin master。

## 13. 项目价值总结

这个项目不是简单地让模型聊天，而是把模型放入一个可控的业务流程：schemas.py 限制输入输出，config.py 集中管理模型，graph.py 明确流程和失败路径，rag.py 提供本地事实，订单和退款服务连接业务工具，checkpoint.py 保存会话，app.py 提供可鉴权和可监控接口，测试和评测持续检查质量。

最终它形成了一个可以继续接入真实订单系统、商品库和生产模型服务的客服智能体基础工程。
